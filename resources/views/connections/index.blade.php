@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-5xl"><div class="mb-8"><p class="text-xs font-black uppercase tracking-[.2em] text-indigo-600">Your circle</p><h1 class="mt-2 text-4xl font-black tracking-tight sm:text-5xl">Connections</h1><p class="mt-2 text-slate-500">People who chose to connect with you.</p></div>
@if($incoming->count())<section class="mb-8"><div class="mb-4 flex items-center justify-between"><h2 class="text-lg font-black">Waves for you</h2><span class="rounded-full bg-pink-50 px-3 py-1 text-xs font-black text-pink-700">{{ $incoming->count() }} waiting</span></div><div class="grid gap-4 sm:grid-cols-2">@foreach($incoming as $connection)<article class="rounded-[1.75rem] border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-center gap-4"><div class="flex h-14 w-14 items-center justify-center rounded-2xl bg-gradient-to-br from-indigo-100 to-pink-100 text-xl font-black text-indigo-700">{{ strtoupper(substr($connection->sender->name,0,1)) }}</div><div class="min-w-0"><p class="font-black">{{ $connection->sender->name }}</p><p class="mt-1 line-clamp-1 text-sm text-slate-500">{{ $connection->sender->bio }}</p></div></div><div class="mt-5 grid grid-cols-2 gap-2"><form method="POST" action="{{ route('connections.accept',$connection) }}">@csrf<button class="tap w-full rounded-2xl bg-slate-950 px-4 py-3 text-sm font-black text-white">Connect</button></form><form method="POST" action="{{ route('connections.decline',$connection) }}">@csrf<button class="tap w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm font-black text-slate-600">Ignore</button></form></div></article>@endforeach</div></section>@endif
<section class="mb-8"><div class="mb-4 flex items-center justify-between"><h2 class="text-lg font-black">Connected</h2><span class="text-xs font-bold text-slate-400">{{ $accepted->count() }} people</span></div><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@forelse($accepted as $connection)@php($person=$connection->sender_id===auth()->id()?$connection->recipient:$connection->sender)<article class="rounded-[1.75rem] border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-center gap-4"><div class="flex h-14 w-14 items-center justify-center overflow-hidden rounded-2xl bg-slate-100 text-lg font-black">{{ strtoupper(substr($person->name,0,1)) }}</div><div><p class="font-black">{{ $person->name }}</p><p class="mt-1 text-xs font-bold text-emerald-600">● Connected</p></div></div><form method="POST" action="{{ route('chat.start',$person) }}">@csrf<button class="tap mt-5 block w-full rounded-2xl bg-indigo-50 px-4 py-3 text-center text-sm font-black text-indigo-700">Message {{ $person->name }}</button></form></article>@empty<div class="rounded-[1.75rem] border border-dashed border-slate-300 bg-white p-10 text-center sm:col-span-2 lg:col-span-3"><div class="text-3xl">♡</div><h2 class="mt-3 font-black">Your circle starts here</h2><p class="mt-2 text-sm text-slate-500">Discover someone nearby and send a wave.</p><a href="{{ route('discover') }}" class="mt-5 inline-block rounded-2xl bg-slate-950 px-5 py-3 text-sm font-black text-white">Discover people</a></div>@endforelse</div></section>
<section><h2 class="mb-4 text-lg font-black">Sent waves</h2><div class="grid gap-3 sm:grid-cols-2">@forelse($outgoing as $connection)<div class="flex items-center justify-between rounded-2xl border border-slate-200 bg-white p-4"><div><p class="font-black">👋 {{ $connection->recipient->name }}</p><p class="mt-1 text-xs text-slate-500">Waiting for a response</p></div><span class="rounded-full bg-amber-50 px-3 py-1 text-xs font-black text-amber-700">Pending</span></div>@empty<p class="text-sm text-slate-500">No pending waves.</p>@endforelse</div></section></div>
@endsection
