@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-3xl"><div class="mb-8"><p class="text-xs font-black uppercase tracking-[.2em] text-indigo-600">Messages</p><h1 class="mt-2 text-4xl font-black tracking-tight sm:text-5xl">Your chats</h1><p class="mt-2 text-slate-500">Private conversations with people you've connected with.</p></div>
<div class="space-y-3">@forelse($conversations as $conversation)@php($person=$conversation->otherUser($user))<a href="{{ route('chat.show',$conversation) }}" class="group flex items-center gap-4 rounded-[1.5rem] border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div class="flex h-14 w-14 shrink-0 items-center justify-center rounded-2xl bg-gradient-to-br from-indigo-100 to-pink-100 text-lg font-black text-indigo-700">{{ strtoupper(substr($person->name,0,1)) }}</div><div class="min-w-0 flex-1"><div class="flex items-center justify-between gap-3"><p class="font-black">{{ $person->name }}</p><span class="text-slate-300 transition group-hover:text-indigo-600">→</span></div><p class="mt-1 truncate text-sm text-slate-500">{{ $conversation->last_message_at?->diffForHumans() ?? 'Start the conversation' }}</p></div></a>@empty<div class="rounded-[2rem] border border-dashed border-slate-300 bg-white p-12 text-center"><div class="mx-auto flex h-16 w-16 items-center justify-center rounded-3xl bg-slate-100 text-2xl">◌</div><h2 class="mt-5 font-black">No conversations yet</h2><p class="mx-auto mt-2 max-w-sm text-sm leading-6 text-slate-500">Make a connection first. Once someone accepts your wave, your private chat starts here.</p><a href="{{ route('discover') }}" class="mt-6 inline-block rounded-2xl bg-slate-950 px-6 py-3 text-sm font-black text-white">Discover people</a></div>@endforelse</div></div>
@endsection
